name: Weekly Deployment Quality Report

on:
  workflow_dispatch:  # ← Add this only for manual runs for now

jobs:
  report:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Calculate DQR
        run: node .github/scripts/calculate-dqr.js
